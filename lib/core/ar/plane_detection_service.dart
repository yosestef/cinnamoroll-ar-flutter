import 'package:ar_flutter_plugin_engine/managers/ar_session_manager.dart';

class PlaneDetectionService {
  void initialize(ARSessionManager sessionManager) {
    // Currently, we rely on PlaneOverlayController for detection states and hit testing.
  }
}
